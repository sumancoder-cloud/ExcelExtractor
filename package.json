{
  "name": "excelextractor",
  "version": "1.0.0",
  "description": "wanna explore everything",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd ExcelExtractor && npm run dev\"",
    "install:all": "npm install --prefix backend && npm install --prefix ExcelExtractor",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix ExcelExtractor",
    "build": "npm install --prefix ExcelExtractor && npm run build --prefix ExcelExtractor"
  },
  "repository": {
    "type": "git",
    "url": "ExcelExtractor"
  },
  "keywords": [
    "nothing"
  ],
  "author": "Tati Suman Yadav",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.11"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
